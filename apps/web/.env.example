# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/vocab-nest

# Auth
JWT_SECRET=change-me-to-a-random-secret

# API (only needed if API is hosted separately)
# NEXT_PUBLIC_API_BASE_URL=/api
